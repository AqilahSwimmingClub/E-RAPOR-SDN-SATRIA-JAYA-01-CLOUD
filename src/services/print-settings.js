import { getSchoolMaster, getTeacherProfile } from './master.js';
import { loadDb, scopeKey, updateDb } from './storage.js';

const MONTHS=['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
function clone(value){return JSON.parse(JSON.stringify(value));}
function clean(value,max=180){return String(value??'').trim().replace(/\s+/g,' ').slice(0,max);}
function key(session){return `${scopeKey(session)}|document-print-settings`;}
export function formatIndonesianPrintDate(dateValue,city='Bekasi'){const raw=String(dateValue||'').trim();if(!raw)return '';const date=new Date(`${raw}T00:00:00`);if(Number.isNaN(date.getTime()))throw new Error('Tanggal cetak tidak valid.');return `${clean(city,80)||'Bekasi'}, ${date.getDate()} ${MONTHS[date.getMonth()]} ${date.getFullYear()}`;}
export function getPrintSettings(session){const school=getSchoolMaster();const teacher=session?.classId?getTeacherProfile(session.classId):null;const saved=loadDb().printSettings?.[key(session)];return clone(saved||{classId:session?.classId||null,semester:session?.semester,academicYear:session?.academicYear,principalName:school.principalName||'',principalNip:school.principalNip||'',teacherName:teacher?.name||'',teacherNip:teacher?.nip||'',city:school.reportCity||'Bekasi',printDate:school.reportDate||'',printDateLabel:school.reportDate?formatIndonesianPrintDate(school.reportDate,school.reportCity||'Bekasi'):''});}
export function savePrintSettings(session,input){if(session?.role!=='teacher'||!session.classId)throw new Error('Hanya Guru yang dapat menyimpan pengaturan cetak rombel.');const date=String(input?.printDate||'').trim();const value={classId:session.classId,semester:session.semester,academicYear:session.academicYear,principalName:clean(input?.principalName),principalNip:clean(input?.principalNip,60),teacherName:clean(input?.teacherName),teacherNip:clean(input?.teacherNip,60),city:clean(input?.city,80)||'Bekasi',printDate:date,printDateLabel:date?formatIndonesianPrintDate(date,input?.city):'',updatedAt:new Date().toISOString()};if(!value.principalName||!value.principalNip||!value.teacherName)throw new Error('Nama Kepala Sekolah, NIP, dan nama Guru wajib diisi.');updateDb(db=>{db.printSettings[key(session)]=value;return db;});return clone(value);}
