node_modules/
dist/
out/
.tools/
# Berkas sementara yang dibuat test lisensi untuk memuat layanan dengan konfigurasi uji.
tests/.tmp/
android/.gradle/
android/build/
android/app/build/
ios/DerivedData/
*.log
# Kunci penandatanganan dan turunannya tidak pernah boleh masuk repository: repository ini
# publik, sehingga siapa pun yang melihatnya bisa menandatangani aplikasi atas nama sekolah.
# Pola di bawah sengaja tidak diawali nama folder supaya berlaku di folder mana pun, termasuk
# ketika berkasnya disalin ke folder utama proyek agar lebih mudah dicari.
signing.properties
*.jks
*.keystore
*.p12
*.pfx
KEYSTORE-CREDENTIALS.txt
# Keluaran npm run signing:secrets --ke-berkas dan cara manual di docs/BUILD-OTOMATIS.md:
# isinya keystore utuh dalam bentuk base64 dan password apa adanya.
secret-*.txt
keystore-base64.txt
release-signing/
release/windows/win-unpacked/
release/windows/builder-debug.yml
release/**/*.blockmap
.DS_Store
Thumbs.db

# Rahasia server lisensi
server/.env
server/secrets/
server/data/
